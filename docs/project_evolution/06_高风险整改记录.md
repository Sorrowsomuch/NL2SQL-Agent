# 高风险整改记录

日期：2026-04-27

## 本轮完成

- 去掉 `config.py` 中数据库密码和 API Key 的明文默认值，默认改为空字符串。
- LLM `api_key` 改为从环境变量读取：
  - `DATAANALYZE_LLM_API_KEY`
  - `DATAANALYZE_EXECUTOR_LLM_API_KEY`
  - `DATAANALYZE_REVIEWER_LLM_API_KEY`
- 修复 `schema_selection.py:tokenize_query()` 中文 n-gram 不可达问题。
- 升级 `db_tool.py` 的 SQL 护栏：
  - 继续只允许单条 `SELECT`
  - 继续禁止注释和危险 DDL/DML
  - 忽略字符串字面量中的 `;` 和危险关键字文本，减少误杀
- 收敛 `reviewer.py` 的规则评审：
  - 空结果不再一律拒绝
  - 单值分析结果不再强制要求图表
  - 对明显可图表化的分析结果仍保留图表要求
- 创建全局 skill：
  - `~/.codex/skills/careful-chinese-coding`

## 验证结果

- `python -m unittest tests.test_schema_metadata tests.test_reviewer_rules tests.test_api_cleanup`
  - `Ran 23 tests ... OK`
- `python -m unittest discover -s tests -p "test_*.py"`
  - `Ran 46 tests ... OK`
- 语法检查
  - `SYNTAX_OK 5 files`
- skill 自检
  - `SKILL_SELF_CHECK_OK`

## 说明

- 这轮没有放松 SQL 只读护栏，只修复了字符串字面量导致的误杀边界。
- 这轮没有破坏 `/chat` 既有响应结构。
- `workflow_events` 继续保留，没有做破坏性删除。
