id: perfetto.table.counter_track
kind: table_profile
title: Perfetto counter_track table
table: counter_track
summary: Counter track metadata, including counter name and optional owning process/thread identifiers.
row_grain: one counter track
important_columns:
  - id
  - name
  - upid
  - utid
join_hints:
  - counter_track.id = counter.track_id
  - counter_track.upid = process.upid
tags:
  - perfetto
  - trace
  - counter
enabled: true
